export enum IdentificationStatus {
    PENDING = 'PENDING',
    APPROVED = 'APPROVED',
    REJECTED = 'REJECTED',
}

export enum IdentificationMethod {
    BIOMETRIC = 'BIOMETRIC',
    ID_CARD = 'ID_CARD',
    PASSPORT = 'PASSPORT',
}
