export interface StepTransparency {
    totalLogs: number;
    verifiedLogs: number;
    rejectedLogs: number;
    unverifiedLogs: number;
    activeDays: number;
    totalDays: number;
    minLogs: number;
    avgConsensusWeight: number;
}
