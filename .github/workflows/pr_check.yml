name: PR check

on:
    pull_request:
        branches:
            - dev
    push:
        branches:
            - devops

jobs:
    lint-and-test:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v5

            - name: Node setup
              uses: actions/setup-node@v5
              with:
                node-version: 24
            
            - name: Install dependencies
              uses: npm install

            - name: Run lint
              uses: npm run lint
            
            - name: Run tests
              uses: npm run test
